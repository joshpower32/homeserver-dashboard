# Copy this file to config.py and fill in your real values:
#     cp config.example.py config.py
#
# config.py is gitignored on purpose — your ntfy topic is effectively a
# public channel (anyone who knows the string can read AND publish to it),
# so it must never be committed.

NTFY_TOPIC   = "your-ntfy-topic-here"   # e.g. joshuadanielca-alerts-passive-monitoring
SERVER_LABEL = "homeserver"